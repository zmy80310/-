/* pages/cart/cart.wxss */
page{
  background: rgb(245, 242, 242)
}
.gologin{
  background-color: #fff;
  padding: 20px 20px;
  display: flex;
  justify-content: space-between;
}
.rig-box{
  color: rgba(0,0,0,.54);
  display: flex;
  justify-content: space-around;
  align-items: center;
  flex-wrap: wrap;
}
.goli{
  font-size: 13px;
}
.right{
  width: 12px;
  height: 12px;
  border-top: 1px solid rgba(0,0,0,.54);
  border-right: 1px solid rgba(0,0,0,.54);
  transform: rotateZ(45deg);

}
.cart-img{
  width: 90px;
  height: 90px;
  background-color: rgba(102, 102, 102, 0.288);
  border-radius: 50%;
  margin: 30px auto;

}
.cart-img>image{
  display: block;
  width: 60%;
  height: 60%;
  margin: 0 auto;
  position: relative;
  top: 50%;
  transform: translateY(-50%);
}
.empty{
  text-align: center;
  margin: 10px 0;
}
.gomi{
  width: 70%;
  margin: 30px auto;
  background-color: #ff6a07e1;
  color: #fff;
  text-align: center;
  border-radius: 3px;
 padding: 10px 0;
}
.cart{
  display: none;
}
 .main-top{
   width: 100%;
   display: flex;
   justify-content: space-between;
   align-items: center;
   padding: 10px;
   background-color: rgba(255, 166, 0, 0.103);
   margin: 8px 0;
 }
 .main-des{
   color: rgba(228, 162, 40, 0.952);
   width: 70%;
   font-size: 13px;
 }
 .main-buy{
   width: 20%;
   text-align: center;
   font-size: 12px;
   color: #fff;
   background: rgba(255, 166, 0, 0.877); 
   padding: 5px 0;
   position: relative;
   right: 25px;
   border-radius: 15px;
 }
 .list{
   background-color: #fff;
   margin: 2px 0;
   padding: 8px 20px;
   display: flex;
   justify-content: space-between;
   align-items: center;
   flex-wrap: wrap;
 }
 .list-img{
   width: 30px;
   height: 30px;
 }
 .list-img>image{
   display: block;
   width: 100%;
   height: 100%;
 }
 .list-name{
   font-size: 14px;
   width: 162px;
 }
 .list-num{
   font-size: 12px;
   color: rgba(0,0,0,.54);
 }
 .list-num>text{
   margin-right: 2px;
 }
 .list-pri{
   color: #ff6700;
   font-size: 13px;
 }
 .settle-body{
   width: 100%; 
   position: fixed;
   bottom: 0px;
 
 }
  .jie,.settle,.item,.settle-body{
   display: flex;
   justify-content: space-between;
   align-items: center;
   flex-wrap: wrap;
     height: 50px
 }
 .settle{
   width: 70%;
   background: #fff;
   box-sizing: border-box;
   padding: 0 15px 0 10px;
 }
 .jie{
   width: 30%;
   background: red;
   color: #fff;
   box-sizing: border-box; 
  padding: 0 24px;

 } 
 .icon{
   position: relative;
   top: 3px;
   margin-right: 5px;
 }
 .active{
      color: #ff6700;
 }
  .sc{
     width: 70rpx;
height: 40rpx;
text-align: center;
line-height: 40rpx;
background-color: red;
font-size: 23rpx;
color: #fff;
border-radius: 15rpx;

  }
/* 重写 checkbox 样式 */
/* 未选中的 背景样式 */
checkbox .wx-checkbox-input{
  border-radius: 50%;/* 圆角 */
  width: 26rpx; /* 背景的宽 */
  height: 26rpx; /* 背景的高 */
}
/* 选中后的 背景样式 （红色背景 无边框 可根据UI需求自己修改） */
checkbox .wx-checkbox-input.wx-checkbox-input-checked{
  border: 1rpx solid #FF783B;
  background: #FF783B;
}
/* 选中后的 对勾样式 （白色对勾 可根据UI需求自己修改） */
checkbox .wx-checkbox-input.wx-checkbox-input-checked::before{
  border-radius: 50%;/* 圆角 */
  width: 20rpx;/* 选中后对勾大小，不要超过背景的尺寸 */
  height: 20rpx;/* 选中后对勾大小，不要超过背景的尺寸 */
  line-height: 20rpx;
  text-align: center;
  font-size:20rpx; /* 对勾大小 30rpx */
  color:#fff; /* 对勾颜色 白色 */
  background: transparent;
  transform:translate(-50%, -50%) scale(1);
  -webkit-transform:translate(-50%, -50%) scale(1);
}